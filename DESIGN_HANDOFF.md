# 디자인 핸드오프 — 서강대 수강신청 클릭 연습 게임

기능/로직 완료. 이 문서는 최초 작성(2026-08-09) 이후 로그인 히어로+광고
배너, 성적표 오마주 결과화면, 다크모드, 구글폰트 페어링 등 여러 세션에
걸쳐 비주얼이 크게 갈아엎어졌는데 문서가 그 상태를 못 따라가고 있었음
— **2026-08-16 Playwright로 실제 화면(데스크톱/모바일/다크모드)을
스크린샷 찍어 현재 상태 기준으로 아래 내용을 다시 정리함.**
파일: `index.html`(구조) + `style.css`(전체 스타일). JS는 건드릴 필요 없음
(디자인만 바꾸는 거면 클래스명/구조를 크게 바꾸지 않는 선에서 작업 권장 —
`js/main.js`가 `document.getElementById`로 특정 id를 참조하고 있음).

## 화면 흐름 (9개, `.screen` 섹션 하나씩)

1. **로그인** (`#screen-login`) — 학번/비번 입력(장식용, 아무 값이나 통과), 서버시간 표시
2. **모드 선택** (`#screen-mode`) — 연타 모드 / 반응속도 모드 버튼 2개
3. **대기(정각 카운트다운)** (`#screen-standby`) — 학번/학년 표시, 대기 배너, 서버시간, 카운트다운 숫자, 입장 버튼, 힌트 문구, 로딩 스피너
4. **대기열 팝업** (`#screen-queue`) — "내 앞의 대기자 수" 카드
5. **과목신청+저장** (`#screen-save`) — 더미 과목 표 + 저장 버튼(우측 상단, 타이밍 맞춰 등장)
6. **로딩** (`#screen-loading`) — 스피너만
7. **완료 토스트** (`#screen-toast`) — 체크마크 + 완료 메시지
8. **결과** (`#screen-result`) — 등급 배지(S/A/B/C/D), 이모지, 등급명/설명, 입장·저장 스탯 바, 개인 최고기록, 공유용 캔버스 이미지, 액션 버튼 3개

## 현재 디자인 톤 (2026-08-16 기준)

- 색: 서강대 공식 서브 컬러 PANTONE 704 C(`--accent: #9e2a2f`) 하나로 통일 + 톤다운된 종이색 배경(`--paper`/`--paper-card`/`--paper-raised`, 순백 아님) + 얇은 보더(`--border`) 위주
- 라이트/다크 모드 지원(`--theme-toggle` 버튼, `prefers-color-scheme` 자동 + `data-theme` 수동 강제, `localStorage`에 선택 저장 — FOUC 방지 인라인 스크립트 있음)
- 타이포그래피: 디스플레이(Noto Serif KR) + 바디(Noto Sans KR) 폰트 페어링을 구글폰트로 로드. 반응형 크기는 `clamp()` 사용(예: `#standby-countdown`, `.mode-half-label`)
- 로그인 화면: 3칸 레이아웃(왼쪽 오마주 일러스트 배경 이미지 `assets/login-bg.webp` / 가운데 로그인 카드 / 오른쪽 무료 광고 배너 3개) — `@media (max-width: 860px)`에서 세로 스택 + 광고는 가로 스크롤 열로 전환
- 결과 화면: 등급 배지(S/A/B/C/D) + 이모지 + 실제 서강대 성적조회표를 오마주한 표(입장속도/저장반응 실전 기록 + 과목별 신청 결과, 초록 체크뱃지, 학기 구분 밴드) — 더 이상 미니멀하지 않음
- 실제 서강대 로고/CI 이미지는 사용하지 않음(의도적 — 상표권/오인 방지). 헤더에 국영문 병기 텍스트로만 "서강대학교" 표기
- 상단에 "⚠️ 비공식 연습용 게임 · 서강대학교 종합정보시스템과 무관" 디스클레이머 고정 노출 — 문구/노출은 유지 필요(브랜딩 리스크 완화용, 임의 삭제 지양)

## 검증 이력 / 알려진 이슈

- **2026-08-16**: Playwright 헤드리스 크로미움으로 로그인→모드선택→대기→대기열→과목신청→결과 전체 흐름을 데스크톱(1440px)/모바일(390px) 뷰포트 + 라이트/다크 모드로 스크린샷 검증. 콘솔 에러 없음.
- **(수정됨, 2026-08-16)** 모드 선택 화면에서 두 버튼(`연타 모드`/`반응속도 모드`) 중 하나에 마우스를 올리면 전역 `button:hover:not(:disabled)` 규칙(`background: var(--accent-dark)`)이 CSS 명시도상 `.mode-half`의 배경 선언을 이겨서 카드 전체가 진한 마룬 단색으로 바뀌고 그 위 어두운 텍스트가 거의 안 보이던 버그. `.mode-half:hover`/`:focus-visible` 규칙에 `background: var(--paper-card)`를 다시 명시해서 고침 — **앞으로 새 버튼 클래스를 추가할 때도 이 패턴(전역 `button:hover` 배경 규칙이 클래스 배경보다 명시도가 높음) 주의할 것.**
- 위 스크린샷 패스에서 그 외 레이아웃 깨짐/가독성 문제는 발견 안 됨. 단, `#screen-toast`(완료 토스트), 860px 이하 반응형 분기(모바일 광고 가로 스크롤 열), `prefers-reduced-motion` 축소 모션은 이번 패스에서 스크린샷으로 별도 확인하지 않았음 — 필요하면 추가 검증.

## 절대 건드리면 안 되는 것 (기능 유지 조건)

- 각 `.screen` 요소는 `id`가 `js/main.js`에서 `document.getElementById`로 직접 참조됨 — id 이름 바꾸면 게임이 깨짐 (바꾸려면 `js/main.js`도 같이 수정 필요)
- `.screen`/`.screen.active`의 `display` 토글 로직(현재 `style.css` 상단)은 절대 다른 화면별 클래스에서 `display`를 재정의하면 안 됨 — 과거에 이걸로 화면이 안 사라지는 버그가 났었음(커밋 로그 `66d018f` 참고)
- `#result-canvas`는 `js/resultCard.js`가 그리는 캔버스 — CSS로 크기만 조절 가능, 내용은 JS가 그림

## 실행/확인

```bash
cd sogang-course-registration-game
npx --yes serve .
```
또는 `run.bat` 더블클릭. `npm test`로 로직 테스트(19개) 확인 가능 — 디자인만 바꾸면 영향 없음.
