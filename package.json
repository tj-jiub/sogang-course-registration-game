{
  "name": "sogang-course-registration-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx --yes vercel dev",
    "start": "npx --yes vercel dev",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/og": "^1.0.1",
    "postgres": "^3.4.9"
  }
}
